# Changelog

## 1.4.0

- 将原先运行效果重复的权限项改为真正独立的三级权限：仅 AstrBot 框架管理员、群主/群管理员及框架管理员、所有人。
- 私聊可用性改为在 Page 中按指令独立设置；命令组子命令默认继承父组设置，子命令显式设置优先。
- 新增私聊禁用和群管理员权限不足的提示开关与可编辑文案；两个提示开关默认关闭并静默拦截。
- 被权限或私聊策略拒绝的指令不会再进入默认 LLM。
- 兼容旧 `admin`、`member`、`everyone` 权限配置，并修复同名 handler 可能修改到错误插件的问题。
- 插件停用或重载时会恢复其他插件原有的权限过滤器。

## 1.3.1

- 修复 Page 页面重复渲染时插件 Logo 因一次性文件令牌失效的问题，改用可重复使用的内嵌图片数据。
- 修复插件名包含空格时 Page API 被重复编码的问题，JM-Cosmos II 等插件的指令现在可以正常加载。

## 1.3.0

- 新增全局指令冷却，可按群组独立限制所有指令的触发间隔。
- 新增命中冷却时的静默忽略和静默排队两种模式。

## 1.2.3

- 修复 Page 页面中删除已有指令别名无效的问题，移除模块脚本无法访问的内联删除事件，并正确处理显式清空后的空别名列表。

## 1.2.2

- 修复权限管理插件列表中的插件 Logo 加载失败问题，改用 AstrBot 文件令牌路径加载 Logo。
- 修复原生 Page API 路由前缀与 `metadata.name` 不一致导致页面数据载入失败的问题。
- 支持修改指令名称和别名后立即应用到运行中的命令处理器，无需重启。
- 优化指令改名交互，改用弹窗表单替代浏览器 prompt。
- 优化权限、别名、改名按钮的前端事件绑定，避免内联事件带来的转义问题。
